echo "running lint"
pnpm lint || {
  echo "lint failed"
  exit 1
}

echo "runnning prettier"

pnpm exec prettier --check . || {
  echo ""
  echo "❌ Code is not formatted."
  echo "💡 Run: pnpm pretty"
  exit 1
}
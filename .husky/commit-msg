pnpm exec prettier --check . || {
  echo ""
  echo "the format is wrong. please run pnpm pretty"
}

pnpm exec commitlint --edit "$1" || {
  echo ""
  echo "❌ Error: Invalid commit message!"
  echo "💡 Tip: use fix: message or feat: message"
  exit 1
}